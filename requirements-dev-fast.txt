# Fast-lane test dependencies — mirrors requirements.txt MINUS the heavy spaCy
# stack (spacy, negspacy, en_core_web_sm). Use this to run the bulk of the
# suite (~108 of 122 files) without paying the multi-minute spaCy + model
# install. The injury-parsing code degrades gracefully when spaCy is absent,
# so pair this with `pytest -m "not spacy"` to skip the ~14 tests that assert
# spaCy-specific behavior.
#
# Keep the pinned versions below in sync with requirements.txt.
fastapi==0.136.1
uvicorn==0.46.0
rapidfuzz==3.14.5
supabase==2.29.0
gotrue==2.12.4
markdown2==2.5.5
openai==2.32.0
sentry-sdk==2.61.0
structlog==25.5.0
python-dateutil==2.9.0.post0
Pillow==12.3.0
python-multipart==0.0.32

# Test tooling
pytest==8.3.5
pytest-xdist==3.6.1
ruff==0.11.10
